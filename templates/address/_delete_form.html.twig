<form method="post" action="{{ path('address_delete', {'idAddress': address.id, 'idStructure' : app.user.structure.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ address.id) }}">
    <button class="button button-light">Supprimer</button>
</form>
