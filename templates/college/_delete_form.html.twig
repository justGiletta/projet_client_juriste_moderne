<form method="post" action="{{ path('college_delete', {'slug': college.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ college.id) }}">
    <button class="button button-light">Supprimer</button>
</form>
